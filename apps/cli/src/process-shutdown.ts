/** Compatibility export for CLI tests and callers of the old app-local path. */
export {
  createProcessShutdown,
  PROCESS_SHUTDOWN_TIMEOUT_MS,
  type ProcessShutdown,
} from '@deepseek-ai/dsh-app-boot'
